# SprintDesk Environment Configuration Example
# Note: The application has built-in defaults in src/utils/constants.ts.
# No environment variables are strictly required to run the application locally or in production.

# Optional API URL overrides:
# VITE_API_BASE_URL=https://dummyjson.com
# VITE_JSON_PLACEHOLDER_URL=https://jsonplaceholder.typicode.com
